import { getAgent } from './client';

export async function getCorpusStats(uri: string) {
  try {
    // Placeholder for Layers XRPC calls
    // Will be implemented when Layers API is live
    return null;
  } catch {
    return null;
  }
}
